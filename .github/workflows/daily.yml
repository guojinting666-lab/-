name: Daily Light Brief

on:
  workflow_dispatch:
  schedule:
    # 00:00 UTC = 08:00 Beijing time
    - cron: "0 0 * * *"

permissions:
  contents: write

jobs:
  send-brief:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate wiki brief and push to WeCom
        env:
          WECOM_WEBHOOK: ${{ secrets.WECOM_WEBHOOK }}
          SILICONFLOW_API_KEY: ${{ secrets.SILICONFLOW_API_KEY }}
          SILICONFLOW_MODEL: Qwen/Qwen2.5-7B-Instruct
        run: |
          python - <<'PY'
          import email.utils
          import hashlib
          import html
          import json
          import os
          import re
          import urllib.parse
          import urllib.request
          import xml.etree.ElementTree as ET
          from datetime import date, datetime, timedelta, timezone

          BEIJING = timezone(timedelta(hours=8))
          STATE_FILE = ".brief_state.json"
          WECOM_WEBHOOK = os.environ.get("WECOM_WEBHOOK")
          SILICONFLOW_API_KEY = os.environ.get("SILICONFLOW_API_KEY")
          SILICONFLOW_MODEL = os.environ.get("SILICONFLOW_MODEL") or "Qwen/Qwen2.5-7B-Instruct"
          SILICONFLOW_URL = "https://api.siliconflow.cn/v1/chat/completions"

          WEEKDAYS = ["星期一", "星期二", "星期三", "星期四", "星期五", "星期六", "星期日"]
          CN_MONTHS = ["一月", "二月", "三月", "四月", "五月", "六月", "七月", "八月", "九月", "十月", "十一月", "十二月"]
          CN_DAYS = ["", "一日", "二日", "三日", "四日", "五日", "六日", "七日", "八日", "九日", "十日", "十一日", "十二日", "十三日", "十四日", "十五日", "十六日", "十七日", "十八日", "十九日", "二十日", "二十一日", "二十二日", "二十三日", "二十四日", "二十五日", "二十六日", "二十七日", "二十八日", "二十九日", "三十日", "三十一日"]
          WEATHER_CODES = {0:"晴",1:"大部晴朗",2:"局部多云",3:"阴",45:"雾",48:"雾凇",51:"小毛毛雨",53:"毛毛雨",55:"较强毛毛雨",61:"小雨",63:"中雨",65:"大雨",71:"小雪",73:"中雪",75:"大雪",80:"阵雨",81:"较强阵雨",82:"强阵雨",95:"雷阵雨"}
          HOLIDAYS_2026 = [("端午节", "2026-06-19"), ("中秋节", "2026-09-25"), ("国庆节", "2026-10-01")]
          SOLAR_TERM_NAMES = ["小寒","大寒","立春","雨水","惊蛰","春分","清明","谷雨","立夏","小满","芒种","夏至","小暑","大暑","立秋","处暑","白露","秋分","寒露","霜降","立冬","小雪","大雪","冬至"]
          SOLAR_TERM_INFO = [0,21208,42467,63836,85337,107014,128867,150921,173149,195551,218072,240693,263343,285989,308563,331033,353350,375494,397447,419210,440795,462224,483532,504758]
          FOOTBALL_LEAGUES = [("世界杯", "fifa.world"), ("欧冠", "uefa.champions"), ("英超", "eng.1"), ("西甲", "esp.1"), ("意甲", "ita.1"), ("德甲", "ger.1"), ("法甲", "fra.1"), ("中超", "chn.1")]
          TECH_RSS = [("OpenAI", "https://openai.com/blog/rss.xml"), ("Apple", "https://www.apple.com/newsroom/rss-feed.rss"), ("Google", "https://blog.google/rss/"), ("Microsoft", "https://blogs.microsoft.com/feed/"), ("GitHub", "https://github.blog/feed/")]
          TEAM_ZH = {"Bosnia-Herz":"波黑","Bosnia and Herzegovina":"波黑","Canada":"加拿大","Paraguay":"巴拉圭","USA":"美国","United States":"美国","Switzerland":"瑞士","Qatar":"卡塔尔","England":"英格兰","Spain":"西班牙","France":"法国","Germany":"德国","Italy":"意大利","Brazil":"巴西","Argentina":"阿根廷"}

          def load_state():
              if not os.path.exists(STATE_FILE):
                  return {"used": {}}
              try:
                  with open(STATE_FILE, "r", encoding="utf-8") as f:
                      return json.load(f)
              except Exception:
                  return {"used": {}}

          def save_state(state):
              for k, v in state.get("used", {}).items():
                  state["used"][k] = v[-800:]
              with open(STATE_FILE, "w", encoding="utf-8") as f:
                  json.dump(state, f, ensure_ascii=False, indent=2)

          STATE = load_state()

          def mark_once(kind, text):
              key = hashlib.sha1(text.encode("utf-8")).hexdigest()
              used = STATE.setdefault("used", {}).setdefault(kind, [])
              if key in used:
                  return False
              used.append(key)
              return True

          def http_get(url, timeout=25):
              req = urllib.request.Request(url, headers={
                  "User-Agent": "DailyLightBrief/1.0 (https://github.com/guojinting666-lab)",
                  "Accept": "application/json,application/xml,text/xml,text/html,*/*",
                  "Accept-Language": "zh-CN,zh;q=0.9,en;q=0.5",
              })
              with urllib.request.urlopen(req, timeout=timeout) as resp:
                  return resp.read().decode("utf-8", errors="replace")

          def post_json(url, payload, timeout=30, headers=None):
              data = json.dumps(payload, ensure_ascii=False).encode("utf-8")
              h = {"Content-Type":"application/json; charset=utf-8"}
              if headers:
                  h.update(headers)
              req = urllib.request.Request(url, data=data, headers=h, method="POST")
              with urllib.request.urlopen(req, timeout=timeout) as resp:
                  return resp.read().decode("utf-8", errors="replace")

          def clean(text):
              text = html.unescape(str(text or ""))
              text = re.sub(r"<style[\s\S]*?</style>", "", text)
              text = re.sub(r"<script[\s\S]*?</script>", "", text)
              text = re.sub(r"<[^>]+>", "", text)
              text = re.sub(r"\[[^\]]*\]", "", text)
              text = re.sub(r"\s+", " ", text).strip()
              return text.strip(" ；;，,。")

          def zh_text(text):
              text = clean(text)
              for en, zh in TEAM_ZH.items():
                  text = text.replace(en, zh)
              return text.replace("Scheduled","待开赛").replace("Final","完场").replace("Halftime","中场").replace("Postponed","延期")

          def translate_short(text):
              if not SILICONFLOW_API_KEY or re.search(r"[\u4e00-\u9fff]", text):
                  return text
              try:
                  raw = post_json(
                      SILICONFLOW_URL,
                      {"model": SILICONFLOW_MODEL, "messages":[{"role":"system","content":"把英文信息翻译成自然中文，只输出译文，不添加事实。"},{"role":"user","content":text}], "temperature":0.1, "max_tokens":180},
                      timeout=60,
                      headers={"Authorization": f"Bearer {SILICONFLOW_API_KEY}"},
                  )
                  return clean(json.loads(raw)["choices"][0]["message"]["content"]) or text
              except Exception:
                  return text

          def summarize_wiki(text, prefix):
              text = clean(text)
              if SILICONFLOW_API_KEY and len(text) > 60:
                  try:
                      raw = post_json(
                          SILICONFLOW_URL,
                          {"model": SILICONFLOW_MODEL, "messages":[{"role":"system","content":"把输入压缩成一句自然中文，不超过55字，只保留事实，不添加新信息。"},{"role":"user","content":text}], "temperature":0.1, "max_tokens":160},
                          timeout=60,
                          headers={"Authorization": f"Bearer {SILICONFLOW_API_KEY}"},
                      )
                      out = clean(json.loads(raw)["choices"][0]["message"]["content"])
                      if out:
                          return f"{prefix}{out}"
                  except Exception:
                      pass
              return f"{prefix}{text[:70]}"

          def date_line(now):
              return f"📆{CN_MONTHS[now.month-1]}{CN_DAYS[now.day]}  {WEEKDAYS[now.weekday()]}"

          def solar_term_for_day(day):
              year = day.year
              base = datetime(1900, 1, 6, 2, 5, tzinfo=timezone.utc)
              for i, name in enumerate(SOLAR_TERM_NAMES):
                  minutes = 525948.76 * (year - 1900) + SOLAR_TERM_INFO[i]
                  term_day = (base + timedelta(minutes=minutes)).astimezone(BEIJING).date()
                  diff = (term_day - day).days
                  if diff == 0:
                      return f"今天是{name}"
                  if 0 < diff <= 15:
                      return f"距离{name}还有{diff}天"
              return ""

          def uv_note(uv):
              if uv is None: return ""
              uv = float(uv)
              if uv < 3: return "紫外线较弱"
              if uv < 6: return "紫外线中等，长时间户外建议防晒"
              if uv < 8: return "紫外线较强，建议防晒"
              return "紫外线很强，尽量减少暴晒"

          def aqi_note(aqi):
              if aqi is None: return ""
              aqi = float(aqi)
              if aqi <= 50: return "空气好，适合户外"
              if aqi <= 100: return "空气可接受"
              if aqi <= 150: return "轻度污染，敏感人群少久待户外"
              if aqi <= 200: return "中度污染，建议减少户外运动"
              return "污染较重，尽量减少户外活动"

          def pm25_note(pm25):
              if pm25 is None: return ""
              pm25 = float(pm25)
              if pm25 <= 35: return "PM2.5较低"
              if pm25 <= 75: return "PM2.5偏高，敏感人群注意"
              return "PM2.5较高，建议减少户外暴露"

          def weather_air_item():
              url = "https://api.open-meteo.com/v1/forecast?latitude=30.2741&longitude=120.1551&daily=weather_code,temperature_2m_max,temperature_2m_min,precipitation_probability_max,uv_index_max,sunrise,sunset&current=wind_speed_10m&timezone=Asia%2FShanghai&forecast_days=2"
              try:
                  obj = json.loads(http_get(url, timeout=20))
                  d = obj.get("daily", {})
                  code = int((d.get("weather_code") or [3])[0])
                  desc = WEATHER_CODES.get(code, "天气待查")
                  high = round(float((d.get("temperature_2m_max") or [0])[0]))
                  low = round(float((d.get("temperature_2m_min") or [0])[0]))
                  pop = (d.get("precipitation_probability_max") or [None])[0]
                  uv = (d.get("uv_index_max") or [None])[0]
                  sunrise = (d.get("sunrise") or [""])[0][-5:]
                  sunset = (d.get("sunset") or [""])[0][-5:]
                  t_code = int((d.get("weather_code") or [3,3])[1])
                  t_desc = WEATHER_CODES.get(t_code, "天气待查")
                  t_high = round(float((d.get("temperature_2m_max") or [0,0])[1]))
                  t_low = round(float((d.get("temperature_2m_min") or [0,0])[1]))
                  advice = "建议带伞" if code in [51,53,55,61,63,65,80,81,82,95] or (pop is not None and int(pop) >= 50) else "适合正常出行"
                  air = ""
                  try:
                      aq = json.loads(http_get("https://air-quality-api.open-meteo.com/v1/air-quality?latitude=30.2741&longitude=120.1551&current=pm2_5,us_aqi&timezone=Asia%2FShanghai", timeout=15)).get("current", {})
                      if aq.get("us_aqi") is not None:
                          air += f"，AQI {round(float(aq['us_aqi']))}（{aqi_note(aq.get('us_aqi'))}）"
                      if aq.get("pm2_5") is not None:
                          air += f"，PM2.5 {round(float(aq['pm2_5']), 1)}（{pm25_note(aq.get('pm2_5'))}）"
                  except Exception:
                      pass
                  extra = []
                  if pop is not None: extra.append(f"降水概率{int(pop)}%")
                  if uv is not None: extra.append(f"紫外线{round(float(uv), 1)}（{uv_note(uv)}）")
                  if sunrise and sunset: extra.append(f"日出{sunrise}，日落{sunset}")
                  return f"天气空气：今天杭州{desc}，{low}℃～{high}℃，{advice}" + ("，" + "，".join(extra) if extra else "") + air + f"；明天{t_desc}，{t_low}℃～{t_high}℃"
              except Exception:
                  return "天气空气：杭州天气暂时获取失败"

          def calendar_item(now):
              today = now.date()
              bits = []
              st = solar_term_for_day(today)
              if st: bits.append(st)
              for name, day in HOLIDAYS_2026:
                  d = datetime.strptime(day, "%Y-%m-%d").date()
                  diff = (d - today).days
                  if 0 <= diff <= 30:
                      bits.append(f"距离{name}还有{diff}天")
                      break
              if now.weekday() == 6: bits.append("今天是周日，明天工作日")
              elif now.weekday() == 4: bits.append("今天是周五，周末前最后一个工作日")
              return "日历节气：" + "；".join(bits) if bits else "日历节气：今天暂无特殊节气或节假日提醒"

          def wiki_current_events(now, limit=3):
              items = []
              for delta in [0, -1]:
                  day = now + timedelta(days=delta)
                  page = f"Portal:Current events/{day.year} {day.strftime('%B')} {day.day}"
                  url = "https://en.wikipedia.org/w/api.php?" + urllib.parse.urlencode({"action":"parse","page":page,"prop":"text","format":"json","formatversion":"2"})
                  try:
                      obj = json.loads(http_get(url, timeout=20))
                      html_text = obj.get("parse", {}).get("text", "")
                      chunks = re.findall(r"<li>(.*?)</li>", html_text, flags=re.S)
                      for raw in chunks:
                          text = clean(raw)
                          if not text or len(text) < 35 or len(text) > 240:
                              continue
                          if any(x in text.lower() for x in ["sports", "football", "cricket", "death", "dies at"]):
                              continue
                          item = summarize_wiki(text, "维基当前事件：")
                          if mark_once("current_event", item):
                              items.append(item)
                          if len(items) >= limit:
                              return items
                  except Exception as e:
                      print("维基当前事件失败：", str(e)[:200])
              return items

          def wiki_on_this_day(now, limit=3):
              items = []
              try:
                  obj = json.loads(http_get(f"https://zh.wikipedia.org/api/rest_v1/feed/onthisday/events/{now.month}/{now.day}", timeout=20))
                  old_first = []
                  rest = []
                  for ev in obj.get("events", []):
                      text = clean(ev.get("text"))
                      year = ev.get("year")
                      if not text or len(text) > 120 or any(x in text for x in ["出生", "逝世"]):
                          continue
                      item = f"历史：{year}年，{text}"
                      if isinstance(year, int) and year <= now.year - 50:
                          old_first.append(item)
                      else:
                          rest.append(item)
                  for item in old_first + rest:
                      if mark_once("history", item):
                          items.append(item)
                      if len(items) >= limit:
                          return items
              except Exception as e:
                  print("历史上的今天失败：", str(e)[:200])
              return items

          def wiki_random_items(limit=5):
              items = []
              bad = ["消歧义", "列表", "年", "月", "日", "行政区", "道路", "车站", "村", "河流"]
              for _ in range(limit * 4):
                  try:
                      obj = json.loads(http_get("https://zh.wikipedia.org/api/rest_v1/page/random/summary", timeout=15))
                      title = clean(obj.get("title"))
                      extract = clean(obj.get("extract"))
                      if not title or not extract or len(extract) < 55:
                          continue
                      if len(title) <= 1 or len(title) > 24 or any(x in title for x in bad):
                          continue
                      item = summarize_wiki(f"{title}：{extract}", "百科：")
                      if mark_once("wiki_random", item):
                          items.append(item)
                      if len(items) >= limit:
                          return items
                  except Exception:
                      continue
              return items

          def parse_pubdate(text):
              try:
                  dt = email.utils.parsedate_to_datetime(text)
                  if dt.tzinfo is None: dt = dt.replace(tzinfo=timezone.utc)
                  return dt.astimezone(BEIJING)
              except Exception:
                  return None

          def football_items(now, limit=2):
              items, seen = [], set()
              for label, league in FOOTBALL_LEAGUES:
                  for delta in [-1,0,1,2]:
                      day = (now + timedelta(days=delta)).strftime("%Y%m%d")
                      url = f"https://site.api.espn.com/apis/site/v2/sports/soccer/{league}/scoreboard?dates={day}"
                      try:
                          obj = json.loads(http_get(url, timeout=15))
                          for event in obj.get("events", [])[:5]:
                              comp = (event.get("competitions") or [{}])[0]
                              teams = comp.get("competitors", [])
                              if len(teams) < 2: continue
                              home = next((x for x in teams if x.get("homeAway") == "home"), teams[0])
                              away = next((x for x in teams if x.get("homeAway") == "away"), teams[1])
                              hn = zh_text(home.get("team", {}).get("shortDisplayName") or home.get("team", {}).get("displayName"))
                              an = zh_text(away.get("team", {}).get("shortDisplayName") or away.get("team", {}).get("displayName"))
                              hs, ass = home.get("score"), away.get("score")
                              st = comp.get("status", {}).get("type", {})
                              state = st.get("state")
                              detail = zh_text(st.get("shortDetail") or comp.get("status", {}).get("displayClock") or "")
                              key = f"{label}{an}{hn}{day}"
                              if key in seen: continue
                              seen.add(key)
                              if state == "post" and hs is not None and ass is not None:
                                  items.append(f"足球：{label} {an} {ass}-{hs} {hn}")
                              elif state == "pre":
                                  items.append(f"足球：{label} {an} vs {hn}，{detail}")
                              else:
                                  items.append(f"足球：{label} {an} {ass or ''}-{hs or ''} {hn}，{detail}")
                              if len(items) >= limit: return items
                      except Exception:
                          continue
              return items

          def steam_item():
              try:
                  obj = json.loads(http_get("https://store.steampowered.com/api/featuredcategories?cc=cn&l=schinese", timeout=18))
                  names = []
                  for x in obj.get("top_sellers", {}).get("items", []):
                      name = clean(x.get("name")).strip("《》")
                      if name: names.append(name)
                      if len(names) >= 3: break
                  if names: return f"游戏：Steam 热销榜前列包括《{'》《'.join(names)}》"
              except Exception:
                  pass
              return None

          def tech_item(now):
              keywords = ["release","launch","announce","security","model","update","发布","推出","上线","安全","模型","更新"]
              for name, url in TECH_RSS:
                  try:
                      root = ET.fromstring(http_get(url, timeout=18))
                      for item in root.findall(".//item")[:8]:
                          title = clean(item.findtext("title"))
                          pub = parse_pubdate(item.findtext("pubDate"))
                          if pub and now - pub > timedelta(days=14): continue
                          if title and len(title) <= 110 and any(k.lower() in title.lower() for k in keywords):
                              return f"科技：{name} 官方更新，{translate_short(title)}"
                  except Exception:
                      continue
              return None

          def build_content():
              now = datetime.now(BEIJING)
              items = [weather_air_item(), calendar_item(now)]
              items.extend(wiki_current_events(now, 3))
              items.extend(wiki_on_this_day(now, 3))
              items.extend(wiki_random_items(5))
              items.extend(football_items(now, 2))
              for optional in [steam_item(), tech_item(now)]:
                  if optional: items.append(optional)
              # Deduplicate exact text while preserving order.
              seen, final = set(), []
              for item in items:
                  if not item or item in seen: continue
                  seen.add(item)
                  final.append(item)
              lines = ["每日轻量晨报", date_line(now)]
              for i, item in enumerate(final[:15], 1):
                  lines.append(f"{i}、{item}；")
              return "\n".join(lines)

          def send_wecom(content):
              if not WECOM_WEBHOOK: raise RuntimeError("缺少 GitHub Secret：WECOM_WEBHOOK")
              raw = post_json(WECOM_WEBHOOK, {"msgtype":"markdown","markdown":{"content":content}})
              print(raw)
              if json.loads(raw).get("errcode") != 0: raise RuntimeError("企业微信返回失败：" + raw)

          print("脚本版本：light-brief-v10-wiki")
          content = build_content()
          print(content)
          send_wecom(content)
          save_state(STATE)
          PY

      - name: Save dedupe state
        run: |
          if [ -f .brief_state.json ] && [ -n "$(git status --porcelain .brief_state.json)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add .brief_state.json
            git commit -m "Update daily brief state"
            git push
          fi
